{% extends 'base.html.twig' %}

{% block body %}
    <h1>Utilisateur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ user.email }}</td>
            </tr>
            
        </tbody>
    </table>

    <a href="{{ path('user.list') }}">back to list</a>

    <a href="{{ path('user.edit', {'id': user.id}) }}">editer</a>

    {{ include('login/_delete_form.html.twig') }}
{% endblock %}
